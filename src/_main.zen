const builtin = @import("builtin");
pub fn panic(msg: []u8, _: ?*builtin.StackTrace) noreturn {
  while (true) {}
}

pub fn main() void {
  _ = add(1, 2); // to link fn add
}

pub export fn add(a: i32, b: i32) i32 {
  return a + b;
}

